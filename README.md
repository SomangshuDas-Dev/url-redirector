# url-redirector
URL Redirector is a lightweight, static redirection project built on GitHub Pages. It automatically redirects visitors to a destination URL defined in a plain text file (url.txt) without exposing the final target to the end user.
